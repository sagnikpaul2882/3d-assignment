export interface Positions3DOptional {
    x?: number,
    y?: number,
    z?: number
}

export interface Positions3D {
    x: number,
    y: number,
    z: number
}

export interface Dimensions2D {
    width: number,
    height: number
}